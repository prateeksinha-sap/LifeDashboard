# Life Dashboard — Python Backend

## Setup

```bash
pip install -r requirements.txt
```

## Run

```bash
uvicorn main:app --reload
```

API will be available at http://localhost:8000
Health check: http://localhost:8000/api/health
